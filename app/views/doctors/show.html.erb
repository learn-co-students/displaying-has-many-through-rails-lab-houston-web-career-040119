<h1>All appointments for <%= @doctor.name %>:</h1>
  <% @doctor.appointments.each do |app| %>
    <p> <%= app.appointment_datetime.strftime("%B %d, %Y at %H:%M") %> </p> <br>
    <p> <%= link_to app.patient.name, patient_path(app.patient) %> </p>
    <% end %>
